<% _.each(glyphs, function(glyph) { %><% _.each(glyph.variants, function(variant) { %>
export { <%= glyph.name %><%= variant.name %> } from './<%= glyph.name %><%= variant.name %>'
<% }); %><% }); %>
