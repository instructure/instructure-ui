export default function ({ colors, typography }) {
  return {
    backgroundColor: colors.lightest,
    textColor: colors.dark,
    fontFamily: typography.fontFamily,
    secondaryTextColor: colors.darkest
  }
}
