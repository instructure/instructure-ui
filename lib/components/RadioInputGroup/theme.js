export default function ({ borders, colors, typography }) {
  return {
    toggleBgColor: colors.dark,
    toggleHandleBgColor: colors.lightest,
    toggleSuccessColor: colors.success,
    toggleDangerColor: colors.danger
  }
}
