export default function ({ colors }) {
  return {
    colorTrackLightBg: colors.colorNeutralInverse,
    colorSpinnerLightBg: colors.colorBrandAccent,
    colorTrackDarkBg: colors.colorBackgroundInverse,
    colorSpinnerDarkBg: colors.colorBrandInverse
  }
}
