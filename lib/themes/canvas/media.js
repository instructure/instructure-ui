import breakpoints from './breakpoints'

export default Object.freeze({
  tabletMin: `min-width: ${breakpoints.tablet}`,
  desktopMin: `min-width: ${breakpoints.desktop}`,
  wideMin: `min-width: ${breakpoints.wide}`
})
