coverage:
  dockerfile: docker/Dockerfile.coverage
  build: .

test:
  build: .
  dockerfile: docker/Dockerfile.test
  env_file:
    - .env.test
  volumes_from:
    - coverage
  volumes:
    - "./.git:/usr/src/app/.git"
