name: VRT

on:
  push:
    branches: [v8]
  pull_request:
    branches: [v8]

  workflow_dispatch:

jobs:
  vrt-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run VRT script
        run: ./docker/vrt
        shell: bash
